# Artifact Gallery — 기능 로드맵

비슷한 도구들(Claude Artifacts, 스니펫 매니저, PrivateBin, Raindrop.io,
Standard Notes/Joplin, CodePen) 조사 후 추린 "녹일 만한 기능" 목록.
하나씩 검토하며 진행한다. 난이도: `[쉬움] [중] [큼]`, ⭐ = 추천.

## ✅ 이미 구현됨
- 비밀번호 기반 E2E 암호화(로컬), 태그 + 필터, 제목 검색, 정렬
- 그리드 썸네일, 뷰어(iframe, blob URL), 추가(붙여넣기/업로드/링크), 편집, 삭제
- 다중 선택 / 일괄 내보내기·삭제, 암호화 백업 export/import
- IndexedDB + gzip 대용량 저장, 파비콘/PWA 아이콘
- 모바일 반응형(그리드 헤더), iOS 자동확대/바운스/줌 잠금 처리

---

## 묶음 1 — 빠른 가치 한 방 `[쉬움]` ⭐ ✅ 완료
- [x] **내용 전체 검색** — 제목 + 태그 + HTML 본문 검색 (WeakMap 캐시)
- [x] **소스 보기 토글** — 뷰어에서 렌더 ↔ 코드 전환 + 코드 복사
- [x] **복제(Fork)** — 편집 모달 "복제" 버튼 (+ 버전 기록에서 "이 버전으로 복제")
- [x] **버저닝** — 편집 시 직전 내용 자동 스냅샷(최근 30개), 버전 기록 모달에서 미리보기/되돌리기/복제
- [x] **자동 잠금 타이머** — ⋯ 메뉴에서 끄기/1/5/15/30분, 미사용·백그라운드 시 잠금 (기본 끄기)

## 묶음 2 — 모바일 프라이버시 `[중]` ⭐
- [ ] **Face ID / 지문 잠금해제 (WebAuthn PRF)**
  - 설계: 비밀번호가 "진짜 키 + 복구 경로", Face ID는 그 기기 전용 "지름길".
    마스터키를 PRF 비밀로 wrap 해서 기기에만 저장. 백업엔 비번 암호화 데이터만.
    미지원 기기/타 기기 복원은 비번 입력으로 폴백. (데이터 손실 위험 없음)

## 묶음 3 — 공유 / 동기화 ⏸ 보류 (저장소 없이 가기로 결정, 2026-06-23)

**결정:** 당분간 **호스트(저장소) 없이** 간다. 동기화는 기존 **암호화 백업
내보내기/복원(수동 파일 이동, AirDrop/메신저)** 으로 충분히 갈음한다.

**기록해 둔 니즈 (나중에 자동 동기화가 필요해지면 다시 검토):**
- 큰 HTML 아티팩트를 **자동으로** 기기 간 옮기려면 **저장 공간(호스트)이 반드시 필요**하다.
  호스트 없이(QR/URL만)는 작은 것만 가능. (분석 결론)
- 용량 한계 분석:
  - QR(단일): 물리적 최대 ~2.9KB, 실전 ~1KB → 데이터 운반 ❌, "링크/키" 운반만.
  - URL `#`프래그먼트(브라우저로 열기): 수십 KB 가능(메신저는 잘림 주의).
  - 키만 URL + 호스트 / 레포: 사실상 무제한.
- "호스트" ≠ "백엔드 서버 직접 운영": **GitHub 레포/gist가 호스트 역할**을 하고
  우리는 **암호문만** 올리므로 E2E·"백엔드 없음" 정신은 유지된다.

**다시 할 때 후보 (난이도순):**
- [ ] **QR 코드 전송** `[쉬움]` — 링크/키를 폰으로 옮기는 보조 (데이터 본체 ❌)
- [ ] **제로지식 공유 링크** `[중]` ⭐ — 키는 URL `#`프래그먼트, 본체는 작은 호스트(서버리스 KV/gist)
- [ ] **레포 기반 E2E 동기화** `[큼]` — GitHub 레포를 암호문 저장소로, 폰↔PC 자동 동기화
      (토큰 로컬 암호화 저장 + 충돌 처리). WebDAV/Dropbox 옵션(Joplin식)도 가능.

## 묶음 4 — 수집 / 입력
- [ ] **북마클릿 / 웹 클리퍼** `[중]` — 보고 있는 페이지를 바로 아티팩트로 저장
- [ ] **Claude 자동 등록** `[중~큼]` — Claude가 만든 아티팩트를 갤러리에 자동 등록
      (MCP 툴/스킬/curl). 동기화 인프라(묶음 3) 위에 얹으면 깔끔.
- [ ] **이미지/스크린샷 썸네일** `[중]`

## 정리 / 보기 (Raindrop·CodePen)
- [ ] **폴더/컬렉션(중첩)** `[중]` — 평면 태그 → 중첩 컬렉션, 한 항목 다중 소속
- [ ] **수동 정렬(드래그) / 즐겨찾기 핀** `[중]`
- [ ] **중복 감지** `[쉬움]` · **깨진 링크 감지(link 타입)** `[쉬움]`
- [x] **버전 히스토리** `[중]` — 수정 시 이전 버전 보관/되돌리기 (묶음 1에서 완료)
- [ ] **라이브 편집 + 분할 미리보기(CodePen식)** `[큼]`

## 폴리시 (Standard Notes·PWA)
- [ ] **오프라인(서비스워커) + 즉시 로딩** `[중]`
- [ ] **라이트 테마 / 테마 선택** `[쉬움]`

---

### 참고 출처
Claude Artifacts(Help Center), Zapier · massCode/daily.dev(스니펫 매니저)
· PrivateBin(GitHub, privatebin.info) · Raindrop.io(Collections)
· Joplin / Standard Notes(E2E 노트) · CodePen(Collections, Live View)
